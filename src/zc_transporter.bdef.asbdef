projection;
strict ( 1 );
use draft;

define behavior for ZC_TRANSPORTER //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Resume;
  use action Discard;
  use action Prepare;
}